{
  "name": "react-use-focus-trap-demo",
  "version": "1.0.0",
  "description": "A minimal demo to debug and test react-use-focus-trap",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "file:../node_modules/react",
    "react-dom": "^17.0.2",
    "react-use-focus-trap": "file:.."
  },
  "devDependencies": {
    "react-scripts": "^5.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
